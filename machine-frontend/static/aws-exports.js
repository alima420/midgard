const awsconfig = {
    Auth: {
        region: 'eu-central-1', // Change this to your region
        userPoolId: 'your_user_pool_id', // Change this to your user pool ID
        userPoolWebClientId: 'your_app_client_id', // Change this to your app client ID
    }
};

export default awsconfig;
